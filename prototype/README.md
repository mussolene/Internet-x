# Legacy Educational Prototype

This directory contains the earlier v0.1 educational UDP/JSON prototype. It is preserved for comparison with the current reference implementation in [`refimpl/`](/Users/maxon/git/me/Internet-x/refimpl/README.md).

Use `refimpl/` for the current runnable baseline.
